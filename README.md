# map method in arrays

```javascript
Array.map((eachElement) => {});
Array.map((eachElement, itsIndex) => {});
Array.map((eachElement, itsIndex, entireArray) => {});
```

### Tasks

- Write javascript code for doubling each mark inside [10,30,50,60,90] ?
- Write javascript code for making each word to `Uppercase` inside ['apple','banana','orange']
- Write javascript code for converting a string to number inside ['90','20','13']

# filter method

```javascript
Array.filter((element) => {});
Array.filter((element, itsIndex) => {});
Array.filter((element, itsIndex, entireArray) => {});
```

### Tasks

- Write javascript code for filtering elements greater than zero [55,26,17,0,23,9,0,81] ?
- Write javascript code for filtering elements that are odd [55,26,17,0,23,9,0,81] ?
- Write javascript code for filtering words whose LENGTH is greater than 6 `["spray", "elite", "exuberant", "destruction", "present"]`?

```javascript
Array.reduce((acc, element) => {});
Array.reduce((acc, element, itsIndex, entireList) => {});
```
